# Mobile QA Scaffold — burnthday.com

**Started:** 2026-07-23 · **Viewport:** 375×812 (iPhone-class) · **Target:** live site + preview branch
**Owner rule:** the lower homepage is single-owner from here — multiple sessions edited it and it shows ("too many cooks"). One person finishes the mobile pass.

Method: DOM overflow probe (element rects vs viewport, skipping anything an ancestor
clips — so "overflow" = a real draggable sideways scroll, not a masked marquee) +
section screenshots. Every finding below has the exact element and a fix direction.

---

## VERDICT

**Not mobile-optimized yet — the homepage's lower half needs a real pass.** Two
ship-critical bugs are already fixed. The rest is one coherent redesign of the
stats → setlists → tour-dates → Nick stack, plus a few contained fixes. Subpages
checked so far are clean; the Song Index is the reference pattern.

Legend: `[x]` done · `[ ]` open · **SEV** HIGH / MED / LOW

---

## 1 · PRIORITY BUILD LIST (Alex's live homepage review, 2026-07-23)

### Shipped (commit b02844f — on the preview branch; needs a `main` deploy to reach the live site)
- [x] **Horizontal left-right rock** — SEV HIGH. `.sheet-scrawl` bled -20px past a
  phone viewport (page → 385px) and the fixed `.home-nav` stretched to match.
  Fix: zero the scrawl's horizontal bleed under 600px. Verified: body + nav = 375.
- [x] **Header hamburger floating off the right rail** — SEV HIGH. Header inherited
  width:calc(100%-40px) + 28px padding = 48px inset. Fix: full-width + 16px gutter
  under 600px. Verified: brand at 16 / hamburger 16 from the right.

### Contained fixes (clear scope, one-owner, next up)
- [ ] **Upcoming bar — TONIGHT badge forces "Los Angeles, CA" onto 3 lines** — SEV MED.
  Move TONIGHT *below* the date in that row so the city gets its width back.
- [ ] **Upcoming-bar arrows aren't design-system** — SEV LOW. The ‹ › / → glyphs there
  don't match the system; reconcile or replace with the standard control.
- [ ] **"Explore Nick's full songbook" broken on mobile** — SEV HIGH (interaction bug).
  Expanding switches the table to a 560px inner-scroll *capped* window (a desktop
  pattern). On mobile that reflows the section and throws the tapped button
  off-screen → reads as "it pushes you down the page." Fix: on mobile, expand
  inline (no inner-scroll cap), or keep the toggle button pinned in view.
- [ ] **Gradle video + the button above it aren't centered** — SEV MED (from-the-stage).

### Lower-homepage mobile redesign (one coherent pass — Alex art-directing)
- [ ] **Excess padding** below the setlist and above the Oakland tour-date row — SEV MED.
- [ ] **The prior-setlist "echo" behind the hero is scrunched/overlapping** at the
  bottom — SEV MED. "Supposed to show on top and at the bottom." **Rec: show 2
  setlists behind, not all 5.**
- [ ] **Stats section is a mess** — SEV HIGH. Too much info, orphan content, and it
  **overlaps on scroll** (sticky section-nav painting over headings + the cramped
  Tour Stats table — see §2).
- [ ] **Nick "Explore the rig" button should stand out** — SEV MED. Move to a top
  corner and give it the **hero Song-Stats-button treatment**.
- [ ] **"2026 setlists" section not showing on mobile** — SEV HIGH. Verify it renders /
  isn't collapsed or pushed off-screen.
- [ ] **Upcoming tour-dates layout not mobile-optimized** — SEV HIGH. "13 shows ahead",
  "Upcoming", and "Full tour" **all need reconsidering** in how they display on mobile.

---

## 2 · HOMEPAGE — section-by-section findings

Page is **24,110px tall** on mobile (the song sheet alone is single-column, ~10,465px).
That length is inherent to showing the full catalog but worth a scan-length rethink.

| Section | Status | Note |
|---|---|---|
| Header | [x] fixed | hamburger seated on the rail (§1) |
| Hero (date/city/nugs/setlist/photo) | good | photo loads fine; a black frame on first paint is a load artifact, not a bug |
| Rarity ticker | LOW / by-design | edge-masked marquee reads clipped on a static glance; matches desktop |
| Marker boards (Sacramento/Oakland ×3) | good | clean 2×2 |
| Prior-setlist echo | [ ] open | scrunched at bottom — show 2 not 5 (§1) |
| Sticky section-nav | [ ] open · HIGH | **paints over every section heading** it lands under (board-intro, Tour Stats, Nick, Setlists) — the "overlaps on scroll" complaint. Fix: scroll-margin/height so the nav never covers the heading row |
| Song sheet | good | single-column, readable; color strikes only on last-4-show songs |
| Tour Stats table | [ ] open · MED | horizontal-scroll, cramped: "Drums"→"Dru…" truncates while "Little By Little" wraps 3 lines; headers cut ("Pla", "Last / us"). **Adopt the Song Index stacked-card pattern** |
| Tour Stats tiles (29/160/572/19.7) | good | clean 2×2 |
| Nick stat tiles | [ ] open · LOW | "531 SONG PLAYS" / "124 IN THE WOODSHED" crowd the label beside the number; stack label under number |
| Nick rig poster + "Explore the rig" | [ ] open | poster great; button needs prominence (§1) |
| Nick songbook expander | [ ] open · HIGH | broken on mobile (§1) |
| From-the-stage (Gradle) | [ ] open | video + button not centered (§1) |
| Upcoming / tour dates | [ ] open · HIGH | not mobile-optimized (§1) |
| 2026 setlists | [ ] open · HIGH | not showing on mobile (§1) — verify |
| Cross-promo cards | good | scrim fix landed; both images read, titles legible |
| Footer | good | 2-col nav, sources, back-to-top |

**Copy (homepage — Alex locked; NOTE ONLY):** Tour Stats intro still ends "…may tell
us something new when they return" (old hedge); footer "…with 2026 tour data and more"
("and more" filler). Both were rewritten for subpages but not the homepage.

---

## 3 · SUBPAGES

| Page | Mobile status |
|---|---|
| /songs/ (Song Index) | **CLEAN — reference pattern.** Table → stacked cards (title / status pill / plays). No overflow. This is how mobile tables should look here. |
| /shelf/ | **CLEAN.** Deck reads well; stat tiles clean 2×2. No overflow. |
| /shelf/updates/ | [ ] not checked |
| /archive/ | **CLEAN.** Real titles (Entry-N junk gone), stacked rows, search works. No overflow. The old "terrible/wayback" version is gone. |
| /tour-in-review/ | probe clean (no overflow); [ ] screenshot pass pending |
| /tour-in-review/<tour> | [ ] not checked (laminate-sheet reuse — table risk) |
| /albums/ + /albums/<one> | [ ] not checked |
| /lyrics-chords/ + a lyric page | [ ] not checked |
| /song-origins/ + an origin page | [ ] not checked |
| /rumors/ | [ ] not checked |
| /about/ | [ ] not checked |
| /faq/ | [ ] not checked |
| /2026/ + a /2026/MM/<setlist> detail | [ ] not checked |

---

## 4 · CROSS-CUTTING (all pages)
- [ ] Tap targets ≥44px — pager arrows, filter chips, nav links (small ones seen: brand 85×38, a 34×19 link).
- [ ] Hamburger menu open state on mobile (never opened this pass).
- [ ] Search overlay (⌘K / tap) on mobile.
- [ ] Landscape + small-tablet (portrait 375 only so far; overflow confirmed present 375–560, clean ≥600).
- [ ] Reduced-motion behavior of ticker + living posters on mobile.

---

## 5 · HOW TO WORK THIS
1. The two shipped fixes are on `claude/project-status-4jp3e7` → preview only. A `main`
   deploy (Alex's word) is what puts them on burnthday.com.
2. Take the **lower-homepage redesign (§1 block 3) as one pass**, not piecemeal — the
   stats/setlists/tour-dates/Nick sections share layout and the churn came from patching
   them separately.
3. Every guarded-markup change ⇒ update `scripts/qa-site.mjs` at equal strength. Gate is
   `npm run qa` (currently 677/677 + 50/50).
4. Verify mobile changes with a real 375px browser (the DOM probe in this doc's method),
   not the embedded preview pane alone.

## Related
- [[handoff-qa-run-2026-07-23]] — QA ownership handoff (lists the phone-pass loop this answers).
- `~/Desktop/qa-pass-checklist.html` — Alex's own autosaving feedback channel.
